---
name: app-bootstrap-cost-estimate
description: Shows account budget alerts tagged to this project via MCP cost_estimate. Does not stop resources. Use when the user types /app-bootstrap-cost-estimate or asks about spend.
disable-model-invocation: true
---

# cost_estimate

Call MCP tool `cost_estimate` on server `app-bootstrap` with `repo` from git origin. See [reference.md](../app-bootstrap/reference.md).
